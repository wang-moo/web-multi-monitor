{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Only the local manager webview receives Tauri core permissions.",
  "webviews": ["main"],
  "permissions": ["core:default"]
}
